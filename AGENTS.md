# AGENTS.md

## 1. 核心工作流：精益流式研发 (ACTR 模型)

废除重前置伪代码设计与长周期 Sprint 锁定，全面采用“**精益契约 + 测试锚定 + 原子闭环**”的流式交付。

[ 1. 意图与边界 (Align) ] ──> [ 2. 契约与测试 (Contract/Test) ] ──> [ 3. 原子闭环 (Task Loop) ] ──> [ 4. Diff 审查 (Review) ]

### 1.1 意图与边界 (Align & Lean Spec)
- **非目标优先 (Non-Goals First)**：对于非平凡任务，优先明确本次**不做什么**，界定系统边界，严禁针对未定义场景进行“预先设计”。
- **精益设计（MVS，不超过 1~2 页）**：
  - 仅包含：**业务意图**、**Non-Goals**、**核心数据结构 (Schema/Struct)**、**1~2 个主链路时序**。
  - **严禁**在设计文档中堆叠超长伪代码或罗列理论上的低概率边缘异常。
- **评审硬性收敛准则 (Bounded Review)**：
  - 设计评审严格限制为 **1 轮**。
  - **原则**：仅拦截死锁、内存破坏、严重并发冲突、ABI 破坏等 **P0/P1 级致命缺陷**。忽略理论扩展性、代码风味与过度防御建议。

### 1.2 契约与测试先行 (Contract & Test First)
- **接口冻结 (Interface Freeze)**：
  - 编码前必须先输出纯虚接口（`interface` / `struct`）、公共头文件及数据契约，并完成对齐。
- **测试锚定 (Spec by Test)**：
  - 将识别出的关键边界与核心路径，直接转化为 **GTest / 单元测试断言**。
  - 机器可执行的测试用例是唯一的“客观收敛标准”，严禁用冗长的自然语言规则替代测试。

### 1.3 原子切片与工具闭环 (Atomic Agent Loop)
- **切片颗粒度**：将大功能拆分为单次 10~30 分钟内可完成、可编译验证的原子任务（Atomic Task），单次修改文件尽量控制在 1~3 个。
- **自愈式构建验证**：
  - 编码完成后，必须自主运行与当前原子任务对应的 CMake target 和测试；Feature 完成时再运行完整 CTest 和全量回归。
  - 遇到编译错误、Sanitizer 报错或测试未通过，**自主根据终端日志与堆栈进行修复**，无需用户干预指导，直至测试全绿。

### 1.4 原子任务执行闸门 (Atomic Task Gates)
- **启动前冻结边界**：开始修改前，`tasks/active_task.md` 必须明确当前原子任务、业务意图、Non-Goals、允许修改的文件、验收命令、时间盒和停止条件。未明确前不得编辑代码。
- **WIP=1 硬约束**：一次只执行 `active_task.md` 指定的一个原子任务。规格中的后续任务、顺手优化和发现的非阻塞问题不得在当前任务中实现。
- **允许文件清单**：所有 patch 必须落在允许修改文件内；每次 patch 后执行 `git diff --name-only` 检查。发现越界文件时立即停止并撤销本次越界修改，不得继续扩大范围。
- **跨任务问题隔离**：发现属于其他原子任务的问题，只记录其目标任务、现象和复现依据；仅当该问题是当前任务的 P0/P1 阻塞缺陷时，才允许在当前任务中处理。
- **时间盒停止**：时间盒到期时只能报告“已完成”“当前错误待修复”或“被明确问题阻塞”三种状态；不得通过增加文件、实现后续任务或扩大测试范围来延长当前任务。
- **完成即停**：当前原子任务达到验收条件后，立即更新任务勾选和 `active_task.md` 状态并停止；不得自动开始下一个原子任务。若需扩大范围，必须先重新拆分任务并更新工作台。
- **请求与工作台冲突时**：用户要求“按规格实现”时，优先遵循 `active_task.md` 的当前边界；只有用户明确要求扩大范围并完成任务重新拆分后，才可修改允许列表。

### 1.5 Diff 审查与完成出口 (Diff Review & DoD)
- **完成的硬性定义 (DoD)**：
  1. 编译通过（零 Error，符合 CMake 入口规范）。
  2. 既有测试与新增测试 100% 绿色通过。
  3. 符合 clang-format 规范且无 Linter 报警。
- **审查与清理**：
  - 仅审查本次改动的 Git Diff。
  - 主动清理自行引入的冗余防御逻辑、死代码及过度抽象。

---

## 2. 任务与规格管理 (3-Tier Task Architecture)

采用“**顶层需求池 $\rightarrow$ 特性规格层 $\rightarrow$ 即时工作台**”三层解耦结构，防止单一文档无限膨胀。

### 2.1 目录结构

```
tasks/
├── product_backlog.md # 顶层需求池（仅记录 Feature 级目标，禁止堆砌子任务）
├── active_task.md # 即时工作台（当前正在执行的单个子任务，即用即抛）
├── specs/ # 特性规格层（一特性一文件，承载 MVS、接口契约与子任务清单）
│ └── feat-<name>.md
├── archive/ # 归档目录（已完成的特性规格移入此处）
└── lessons.md # 普适性工程原则（严格控制体积）
```

### 2.2 任务生命周期与流转
1. **特性立项**：
   - 从 `product_backlog.md` 选取需求，在 `specs/feat-<name>.md` 中编写极简 MVS、接口契约与 3~5 个子任务清单（Task Breakdown）。
   - `product_backlog.md` 仅做状态标记（`[ ]` 待办、`[-]` 进行中、`[x]` 已完成），不记录微观子任务进度。
2. **单任务执行 (WIP = 1)**：
   - 将当前聚焦的单个子任务载入 `active_task.md`（作为临时工作台）。
   - 在工作台中冻结允许修改文件、验收命令、时间盒和停止条件；执行期间不得越过该边界。
   - 执行“编码 $\rightarrow$ 编译 $\rightarrow$ 测试”原子闭环。
   - 每次修改后检查 `git diff --name-only`，只审查和保留当前任务允许范围内的 diff。
3. **完成与归档**：
   - 子任务通过 DoD 后，在对应的 `specs/feat-<name>.md` 中勾选完成。
   - 只勾选实际完成的当前原子任务；未执行的后续任务不得提前更新状态。
   - 当前原子任务完成后停止，不自动进入下一个任务。
   - 整个 Feature 全部完成并通过验收后，将该 `specs/feat-<name>.md` 移入 `tasks/archive/`，并在 `product_backlog.md` 中标记完成。

### 2.3 经验教训管理 (`lessons.md`)
- 只有暴露了跨任务、跨模块可复用的架构/工程方法论时，才写入 `tasks/lessons.md`。
- 严禁将一次性事实、具体临时路径、端口、单次编码失误写入规则；新增时优先合并/改写旧规则，定期瘦身。

---

## 3. 架构原则

1. **统一框架（Python Worker 例外）**：C++ 服务都是同一个框架程序加载不同 `.so` 运行起来的，彼此对等；Python Worker 是独立的 FastAPI 进程。
2. **IPlugin 机制保留**：所有功能模块以 `.so` 插件形式加载。
3. **控制面 HTTP 统一**：服务间控制面通信全部走 HTTP + URI 路由。
4. **数据面独立通道**：高吞吐数据传输走共享内存 / Arrow IPC，不经过 HTTP。
5. **Interface 思维**：Plugin 通过 `interface`（纯虚基类）向本进程内的其他 Plugin 暴露能力，调用方通过 `IQuerier` 按 `IID` 查找，不直接依赖具体实现类。
6. **IPlugin 生命周期批次调用**：插件加载必须分阶段批次执行——先所有插件 `Option()`，再所有插件 `Load()`，最后所有插件 `Start()`。禁止逐个插件走完整个生命周期（Option→Load→Start 再下一个），因为 `Start()` 时必须保证所有插件的接口已通过 `Load()` 注册完毕。

---

## 4. 工程与工具链规范

### 4.1 构建与编译入口
- **仓库根目录**：固定为 `flowSQL`（大小写敏感）。
- **构建入口**：默认入口固定为 `src/CMakeLists.txt`。
- **标准编译命令**：
  - 配置：`cmake -B build src`
  - 编译：`cmake --build build -j$(nproc)`
  - 禁止通过搜索 `CMakeLists.txt` 来猜测入口。

### 4.2 Worktree 与依赖缓存
- `git worktree` 开发统一复用主工作区的 `.thirdparts_installed` 和 `.thirdparts_prefix`，禁止每个 worktree 各自维护一套第三方编译缓存。
- 新增 `thirdparts/*-config.cmake` 或新增第三方依赖时，必须先在主工作区完成一次下载 / 编译 / 安装缓存，再进入 `.worktrees/` 分支开发。
- 头文件库（如 `Eigen 3`）同样遵循此规则，严禁跳过主工作区缓存预热。

### 4.3 版权声明
- 新建或修改 `*.h/*.hpp/*.cpp/*.cc` 文件时，文件头必须使用统一版权声明：
  ```cpp
  // Copyright (C) 2026 LIHUO. All rights reserved.
  // Licensed under the MIT License.

---

## 5. 编程风格与代码规范

- 语言标准：C++17。
- 命名空间：主命名空间为 flowsql（子空间如 flowsql::protocol）。
- 代码格式化：采用 src/.clang-format（基于 Google 风格，4 空格缩进，120 列宽）。
- 接口宏：interface 宏定义为 struct（见 common/define.h），用于声明纯虚接口。
- 简洁与正交：严格遵循 KISS 与 YAGNI 原则，不编写未经测试验证支撑的“防御性死代码”；保持代码最小影响面。

---

## 6. 交互纪律与排查红线

- 中文交流：全程使用中文交流。
- 自主编译：需要编译或运行测试时直接执行，无需向用户请求确认。
- 危险操作确认：移动或删除目录前必须由用户明确确认。
- 提交控制：严禁自动发起 git commit 或 git push，等待用户明确指令后再执行提交。
- 问题排查必须先核查再修改（强制红线）：
- 事实核查先行：收到缺陷或问题反馈后，必须先查日志、看堆栈、验证测试复现或检查代码路径，严禁未经核查直接改代码。
  - 确认是问题：先输出最小修改方案（影响面、风险、验证命令），经确认后实施修改。
  - 确认非问题：必须提供可复核的客观证据（命令输出、日志、接口返回等）并说明原因。
